name: PRTP
version: "${version}"
main: xyz.distemi.prtp.PRTP
softdepend:
  - Vault
  - PlaceholderAPI
authors: [ Distemi ]
description: RTP by profiles
prefix: "Prototype RTP"
commands:
  prtp:
    aliases:
      - rtp
    description: "§aRandom teleport"
    usage: "§a/<command> <profile>"
    permission: prtp.use
permissions:
  prtp.use:
    default: true
  prtp.reload:
    default: op
